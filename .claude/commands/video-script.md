---
description: Сгенерировать сценарий видео и телесуфлёр-DOCX в .tmp/teleprompter/. Аргумент — тема ролика и опционально дата+время публикации.
---

Запусти skill `video-script` через Skill tool, передав в него аргументы пользователя ($ARGUMENTS).

Контекст для модели:
- Аргумент `$ARGUMENTS` может содержать: только тему («бутылочное горлышко в закупках»), тему + детали кейса, или тему + дату/время публикации (express mode: «про тендеры в нефтянке, публикация 2026-05-01 14:00»).
- Если темы нет — спроси через AskUserQuestion.
- Если даты/времени публикации нет — обязательно спроси (нужно для имени DOCX `YYYY-MM-DD-HHMM-alias.docx`).
- Дальше следуй workflow из skill SKILL.md: tone-of-voice → структура (хук/тезис/теория/кейс/резюме) → утверждение → телесуфлёр-текст → сохранить DOCX в `.tmp/teleprompter/`.
